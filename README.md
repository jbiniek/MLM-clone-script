# MLM-clone-script
Automatic identity regeneration for cloned Multi-Linux Manager clients.

Use as a standalone script and run before bootstrapping or add at the **beginning** of your bootstrap script (right below #!/bin/bash).

Solves various issues that can occur when connecting cloned machines.
